# Build-time config — values are baked into the JS bundle at build time.
# PUBLIC ONLY: do not put secrets here; use the runtime config.json instead.
VITE_APP_TITLE=[[ project_name ]]
VITE_API_BASE_URL=http://localhost:8000
